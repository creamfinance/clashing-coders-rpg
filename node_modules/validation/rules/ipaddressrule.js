var range_check = require('range_check');
var IpAddressRule = Class.bind(null, 'IpAddressRule', ValidationRule);

IpAddressRule({
    constructor: function () {
        
    },
    validate: function (context, fullData, currentValue, callback) {
        // validate ipv4 or ipv6 address
        var ipValid = range_check.validIp(currentValue);

        if (ipValid) {
            callback(true);
        } else {
            context.errors.push('Invalid IP Address: ' + currentValue);
            callback(false);
        }
    }
});